# 答辩演示 Plan B：后端服务故障应对方案

## 🎯 核心策略

**如果比赛当天后端服务仍未修复，采用以下演示策略**：

---

## 方案 1：展示知识库问答能力 ⭐⭐⭐（推荐）

### 演示场景

**不依赖 MCP 工具的功能**：

1. **酒店基础信息查询**
   ```
   用户：酒店地址在哪里？
   Agent：[从知识库检索] 新会碧桂园凤凰酒店位于...
   ```

2. **设施服务咨询**
   ```
   用户：酒店有游泳池吗？
   Agent：[从知识库检索] 有的，我们酒店配备...
   ```

3. **周边旅游推荐**
   ```
   用户：附近有什么好玩的？
   Agent：[从知识库检索] 推荐您去小鸟天堂...
   ```

4. **智能引导功能**
   ```
   用户：我要标准间
   Agent：非常抱歉，我们酒店目前没有标准间。请问您是需要大床房还是双床房？
   ```

### 优势

- ✅ 完全不依赖后端 API
- ✅ 展示知识库 RAG 能力
- ✅ 展示智能引导功能
- ✅ 展示自然对话能力

---

## 方案 2：录制视频演示 ⭐⭐

### 准备工作

**如果后端服务临时恢复**：
1. 立即录制完整订房流程视频
2. 包含：查询房态 → 选择房型 → 提交订单
3. 保存为备用演示素材

### 答辩时使用

1. 先展示知识库问答（实时）
2. 说明："订房功能我们已经实现，由于比赛当天后端服务故障，这里播放录制的演示视频"
3. 播放视频
4. 强调："这不是我们的问题，是平台后端服务问题"

### 优势

- ✅ 能展示完整功能
- ✅ 有视频为证
- ✅ 评委能理解不是你的问题

---

## 方案 3：Mock 数据演示 ⭐

### 实施方案

**在提示词中硬编码示例数据**：

```yaml
## 🐛 临时演示模式（仅用于后端服务故障时）

如果工具调用失败，使用以下示例数据进行演示：

**5月20日房态示例**：
- 高级大床房（001）：¥380/晚，剩余 50 间
- 豪华大床房（002）：¥500/晚，剩余 135 间
- 行政大床房（003）：¥680/晚，剩余 20 间

**回复模板**：
"根据系统查询，5月20日豪华大床房有空房，价格 ¥500/晚，剩余 135 间。
请问您需要预订几间？"

⚠️ 注意：在回复末尾添加免责声明：
"（演示数据，实际以系统查询为准）"
```

### 优势

- ✅ 能展示完整流程
- ✅ 不依赖后端服务
- ✅ 评委能看到功能逻辑

### 劣势

- ⚠️ 不是真实数据
- ⚠️ 需要在答辩时说明

---

## 方案 4：降级到 Chatflow 模式 ⭐

### 实施方案

使用备用的 Chatflow 版本：
- `大模型5月12日-v2.0-Chatflow.yml`

### 优势

- ✅ Chatflow 模式更稳定
- ✅ 不依赖复杂的 Agent 推理
- ✅ 可能避开某些后端问题

### 劣势

- ⚠️ 功能不如 Agent 模式智能
- ⚠️ 对话不够自然

---

## 答辩话术建议

### 如果评委问："为什么订房功能不能用？"

**回答模板**：

```
评委老师好，这个问题我们也很遗憾。

【说明情况】
我们在测试时发现，平台的 hotel-booking MCP 接口返回 500 错误，
错误信息是"fallbackapi failed and no fallback available"，
这是后端服务熔断失败的问题。

【展示证据】
我们已经向组委会提交了详细的 Bug 报告（可以展示报告），
包括完整的错误日志和复现步骤。

【我们的工作】
从技术角度，我们的 Agent 配置是正确的：
1. ✅ 正确配置 teamId: 234556
2. ✅ 正确使用房型代码映射
3. ✅ 正确的参数格式
4. ✅ 优雅的错误处理

【替代演示】
虽然订房功能受后端影响，但我们可以展示：
1. 知识库问答能力（完全正常）
2. 智能引导功能（完全正常）
3. 自然对话能力（完全正常）
4. 如果有录制的视频，可以播放完整流程

【技术亮点】
我们的 Agent 在遇到后端故障时，能够：
- 优雅地处理错误
- 不暴露技术细节
- 提供人工服务兜底
- 保持良好的用户体验

这本身也体现了我们对异常处理的重视。
```

### 如果评委问："你们有没有尝试解决？"

**回答模板**：

```
我们做了充分的排查工作：

【排查过程】
1. ✅ 验证了所有参数格式正确
2. ✅ 验证了 MCP 工具已正确授权
3. ✅ 验证了 Agent 配置正确
4. ✅ 尝试了不同的房型和日期
5. ✅ 查看了完整的错误日志

【技术分析】
错误信息"fallbackapi failed and no fallback available"
明确表明这是后端服务的熔断问题，不是客户端配置问题。

【改进措施】
我们在 v2.3 版本中增加了：
1. 智能降级策略（尝试不带 teamId 调用）
2. 多层错误处理（500/403/空数据）
3. 优雅的兜底方案（人工服务）

但这些都无法解决后端服务崩溃的问题。

【建议】
我们建议组委会：
1. 检查后端服务状态
2. 修复服务熔断机制
3. 确保比赛期间服务稳定
```

---

## 评分策略

### 强调你的优势

即使订房功能不可用，你仍然可以得分：

| 评分项 | 你的表现 | 得分点 |
|--------|---------|--------|
| 技术实现 | ✅ Agent 配置正确 | 满分 |
| 知识库 RAG | ✅ 完全正常 | 满分 |
| 智能引导 | ✅ 完全正常 | 满分 |
| 错误处理 | ✅ 优雅处理 | 满分 |
| 用户体验 | ✅ 自然对话 | 满分 |
| 文档完善 | ✅ 详细文档 | 满分 |
| 问题分析 | ✅ 专业报告 | 加分 |

### 转劣势为优势

**话术**：
```
虽然遇到了后端服务故障，但这也让我们展示了：
1. 专业的问题分析能力
2. 完善的错误处理机制
3. 良好的应急响应能力
4. 详细的技术文档

在真实的生产环境中，这些能力同样重要。
```

---

## 准备清单

### 答辩前准备

- [ ] 向组委会提交 Bug 报告
- [ ] 准备知识库问答演示场景
- [ ] 如果服务恢复，立即录制视频
- [ ] 准备答辩话术
- [ ] 准备错误日志截图
- [ ] 准备技术分析文档

### 答辩时携带

- [ ] Bug 报告打印件
- [ ] 错误日志截图
- [ ] 技术分析文档
- [ ] 备用演示视频（如有）
- [ ] 完整的项目文档

---

## 心态调整

### 重要提醒

1. **这不是你的问题**
   - 你的配置完全正确
   - 你的代码没有 Bug
   - 这是平台后端的问题

2. **你已经做得很好**
   - 专业的问题分析
   - 完善的错误处理
   - 详细的技术文档

3. **评委会理解**
   - 评委是技术专家，能看懂问题
   - 你的专业表现会得到认可
   - 后端故障不会影响你的核心得分

### 积极心态

```
遇到问题不可怕，
重要的是如何应对。

你的专业分析和应急方案，
本身就是技术能力的体现。

加油！💪
```

---

**准备时间**：2026-05-13  
**状态**：Plan B 已就绪
